<!-- Navbar -->
<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <a class="navbar-brand" href="/">Hawkerpreneur</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Posts",   '/microposts' %></li>
          <li><%= link_to "Community",   '/users' %></li>
        <% end %>
        <li><%= link_to "Hawkers",   '/hawkers' %></li>
        <li><%= link_to "Info & News",   '/info' %></li>
        <li><%= link_to "Contact",   '/contact' %></li>
        <li>
          <% if user_signed_in? %>
            <%= button_to 'LOGOUT', destroy_user_session_path, :method => :delete, :class => 'btn btn-lg' %>
          <% else %>
            <%= button_to 'LOGIN', '/users/sign_in', :class => 'btn btn-lg' %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>